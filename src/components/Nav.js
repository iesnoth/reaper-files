//nav is a child of App and should always appear at the top.
//will need to use react-router-dom for nav

const Nav = () => {
    return(
        <div>
         <h1>Navbar</h1>
        </div>
    )
}

export default Nav